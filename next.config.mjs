/** @type {import('next').NextConfig} */
const nextConfig = {
  /* Hide Next.js dev/build indicator (circular N logo) in desktop and all views */
  devIndicators: false,
};

export default nextConfig;
